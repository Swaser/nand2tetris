class Leer {
    field int a;
    static char b;

    function int bla(Rug a32, int five) {

        var boolean h;
        if (h) {
            var char c;
            var int d;
            let d = 15;
            return 3;
        }

        while (h) {
            var int a32;
        }

        var int f;
        var int g;
        let f = g;
        let h = f < g;

        var int x;
    }

    constructor void Leer() {

    }
}

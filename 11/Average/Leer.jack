class Leer {
    field int a;
    static char b;

    method int bla(Rug a32, int five) {

        var boolean h;
        var char c;
        var int d,f,g;

        if (h) {
            let d = 15;
            return 3;
        }

        let b = 13;

        while (h) {
            let f = d * g + f;
        }

        let f = g;
        let h = f < g;

        return g;
    }

    constructor void Leer() {
        return;
    }
}

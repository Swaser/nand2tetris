class Leer {
    field int a;
    static char b;

    function int bla(Rug a32, int five) {

        var boolean h;
        var char c;
        var int d,f,g;

        if (h) {
            let d = 15;
            return 3;
        }

        while (h) {
        }

        let f = g;
        let h = f < g;

        return g;
    }

    constructor void Leer() {

    }
}
